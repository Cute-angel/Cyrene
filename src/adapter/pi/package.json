{
  "name": "@cyrene/pi-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cyrene memory adapter for Pi",
  "scripts": {
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "pi": {
    "extensions": ["./src/extension.ts"],
    "prompts": ["./prompts"]
  },
  "keywords": ["pi-package"],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
